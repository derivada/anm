#include <stdio.h>

void main(){
    int n, i;
    float* a, aa, s, ss, b, u, r, bp, bs, v;
    float aux;

    printf("Orden del sistema: ");
    scanf("%d", &n);
    malloc(a, n*sizeof(int));
    printf("");
}